// The address the contact form has always delivered to (BUSINESS_OWNER_EMAIL on
// the notification service). Shown and used for mailto: while ordering is off,
// so a message never silently disappears into a dead API.
export const CONTACT_EMAIL = "gmpetitcafe@gmail.com";
